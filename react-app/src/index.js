import React from "react";
import ReactDOM from "react-dom";


// this  "element" is an Object that acts as a lightweight DOM
const element = <h1>Hello World</h1>; 
ReactDOM.render(element, document.getElementById("root"))